# xa-ppt-skill 需求说明

## 1. 产品定位

xa-ppt-skill 是一款 Claude Code skill，采用**组件-布局-主题**设计系统生成图片嵌入式 PPT。

核心价值：用户用自然语言描述需求，AI 在设计系统约束内自由发挥，生成专业级演示文稿。

## 2. 目标用户

- **主要用户**：开发者
- **使用场景**：技术分享、架构展示、产品介绍、汇报演示

## 3. 设计系统

### 3.1 三层结构

```
主题 (Theme) → 约束颜色、字体、规则
布局 (Layout) → 区域结构
组件 (Component) → 可用元素
```

### 3.2 主题

| 主题 | 适用场景 | 视觉特征 |
|------|----------|----------|
| tech | 技术分享、架构展示 | 深色背景 + 蓝色调 |
| simple | 商务汇报、介绍 | 浅色背景 + 深色文字 |
| minimal | 极简展示 | 纯白 + 黑色文字 |

AI 根据内容语义自动匹配主题。

### 3.3 布局（按语义分类）

| 类型 | 布局 | 说明 |
|------|------|------|
| 封面 | cover | 大标题居中，无页码 |
| 结尾 | ending | 结束页，无页码 |
| 目录 | toc | 目录页 |
| 总结 | summary | 居中展示要点 |
| 正文 | content | 标题+列表 |
| 正文 | two-column | 双栏对比 |
| 正文 | grid | 2x2 网格 |

AI 根据内容类型选择布局。

### 3.4 组件

| 类型 | 组件 |
|------|------|
| 文本 | h1, h2, h3, p, text |
| 列表 | ul, ol |
| 代码 | code, pre |
| 媒体 | img, chart |
| 引用 | quote |
| 布局 | divider, card, flex, grid |

## 4. 设计理念

```
约束边界内，AI 自由发挥
```

- **主题定义边界**：颜色范围、字号范围、禁止事项
- **AI 自主决定**：具体的样式值、装饰选择、排版微调
- **保证一致性**：同一主题下生成的内容风格统一

## 5. 工作流程

```
用户描述需求（自然语言）
       │
       ▼
AI 对话理解需求
       │
       ▼
AI 生成大纲 → 用户确认
       │
       ▼
AI 选择布局 + 匹配主题
       │
       ▼
AI 生成 HTML（组件组合）
       │
       ▼
AI 自检（符合设计系统约束）
       │
       ▼
脚本渲染截图（Playwright）
       │
       ▼
脚本组装 PPT（python-pptx）
       │
       ▼
输出 .pptx
```

## 6. 分工边界

### AI 负责（skill.md 指导）
- 理解用户自然语言需求
- 生成结构化大纲（用户确认）
- 选择合适布局
- 匹配主题风格
- 生成 HTML 内容
- 自检符合设计系统约束

### 脚本负责（Python）
- `render.py`：Playwright 渲染截图
- `build_pptx.py`：python-pptx 组装 PPT

## 7. 输入与输出

### 输入
- **格式**：自然语言描述 + 可选参考材料
- **示例**：
  ```
  用户：帮我做一个微服务架构的技术分享 PPT
  ```

### 输出
- **格式**：.pptx 文件
- **规格**：16:9 宽屏（1920×1080）
- **类型**：全图型 PPT（每页为嵌入图片）

## 8. 自检清单

AI 生成 HTML 后检查：

- [ ] 背景色符合主题
- [ ] 字号在允许范围内
- [ ] 颜色不超过 3 种
- [ ] 每页不超过 5 个要点
- [ ] 没有使用禁止的样式

## 9. 当前版本范围

### 能做什么
- 自然语言需求理解
- AI 生成大纲并确认
- 设计系统自动匹配（主题+布局）
- HTML 幻灯片生成（约束内自由发挥）
- 截图与 PPT 组装
- 支持将收尾页拆分为「总结页（时间线）」+「致谢页」
- 单页要点超过 5 条时，自动收紧版式，避免页码被挤出

### 不能做什么
- 不支持用户自定义模板（当前版本）
- 不支持导出可编辑文字
- 不支持多语言 PPT
