# xa-ppt-skill 需求分析

## 1. 分析目标

将自然语言需求转换为图片嵌入式 PPT，采用**组件-布局-主题**设计系统。

## 2. 设计系统

### 2.1 三层结构

```
主题 (Theme) → 颜色、字体范围、规则约束
布局 (Layout) → 区域结构、排列方式
组件 (Component) → 可用元素、样式约束
```

### 2.2 主题

| 主题 | 适用场景 | 视觉特征 |
|------|----------|----------|
| tech | 技术分享、架构展示 | 深色背景 + 蓝色调 |
| simple | 商务汇报、介绍 | 浅色背景 + 深色文字 |
| minimal | 极简展示 | 纯白 + 黑色文字 |

### 2.3 布局（按语义分类）

| 类型 | 布局 | 说明 |
|------|------|------|
| 封面 | cover | 大标题居中，无页码 |
| 结尾 | ending | 结束页，无页码 |
| 目录 | toc | 目录页 |
| 总结 | summary | 居中展示要点 |
| 正文 | content | 标题+列表 |
| 正文 | two-column | 双栏对比 |
| 正文 | grid | 2x2 网格 |

### 2.4 组件

| 类型 | 组件 |
|------|------|
| 文本 | h1, h2, h3, p, text |
| 列表 | ul, ol |
| 代码 | code, pre |
| 媒体 | img, chart |
| 引用 | quote |
| 布局 | divider, card, flex, grid |

## 3. 系统边界

```
用户输入（自然语言）
       │
       ▼
┌─────────────────────────────────────┐
│         AI（skill.md 指导）          │
│  ├─ 需求理解                         │
│  ├─ 大纲生成（用户确认）              │
│  ├─ 选择布局                         │
│  ├─ 匹配主题                         │
│  ├─ 生成 HTML（组件组合）             │
│  └─ 自检（符合设计系统约束）          │
└─────────────────────────────────────┘
       │
       ▼ HTML 文件
┌─────────────────────────────────────┐
│         脚本（Python）               │
│  ├─ render.py：截图                  │
│  └─ build_pptx.py：组装 PPT          │
└─────────────────────────────────────┘
       │
       ▼
    .pptx 文件
```

## 4. AI 能力需求

### 4.1 需求理解
- 对话理解用户意图
- 提取主题、关键词、类型

### 4.2 大纲生成
- 生成结构化大纲
- 用户确认/修改后继续
- 每页包含：布局建议、内容要点

### 4.3 布局选择
- 根据内容结构选择布局
- 布局选择规则：
  - 单主题展开 → title-content
  - 两类对比 → two-column
  - 4 个并列要点 → grid-2x2
  - 标语/大引用 → center
  - 章节封面 → fullscreen

### 4.4 主题匹配
- 技术类内容 → tech
- 商务类内容 → simple
- 极简展示 → minimal
- 匹配规则基于关键词

### 4.5 HTML 生成
- 读取主题配置（颜色、字号范围、规则）
- 读取布局配置（区域结构）
- 组合组件生成 HTML
- 应用主题约束

### 4.6 自检
- 背景色符合主题
- 字号在允许范围内
- 颜色不超过 3 种
- 每页不超过 5 个要点
- 没有使用禁止的样式

## 5. 脚本能力需求

### 5.1 render.py
- Playwright 无头渲染
- 截图分辨率 1920×1080
- 输出 PNG 格式
- 输入：HTML 文件列表
- 输出：PNG 文件列表

### 5.2 build_pptx.py
- python-pptx 组装
- 16:9 比例（10in × 5.625in）
- 每页图片铺满
- 输入：图片列表 + 标题
- 输出：.pptx 文件

## 6. 数据契约

### 阶段：用户输入
```
用户：帮我做一个微服务架构的技术分享 PPT
```

### 阶段：AI 生成的大纲（待确认）
```json
{
  "title": "微服务架构技术分享",
  "slides": [
    {
      "layout": "fullscreen",
      "content": ["微服务架构"]
    },
    {
      "layout": "title-content",
      "title": "概述",
      "content": ["什么是微服务", "微服务 vs 单体架构"]
    },
    {
      "layout": "two-column",
      "title": "对比",
      "left": ["单体架构特点"],
      "right": ["微服务架构特点"]
    },
    {
      "layout": "grid-2x2",
      "title": "核心组件",
      "zones": {
        "ZONE-A": ["服务拆分"],
        "ZONE-B": ["API 网关"],
        "ZONE-C": ["服务通信"],
        "ZONE-D": ["容器化部署"]
      }
    }
  ]
}
```

### 阶段：HTML 文件
- 目录：`output/slides/`
- 每页一个 HTML，1920×1080，内联样式
- 包含 data-layout 属性

### 阶段：截图
- 目录：`output/images/`
- PNG 格式，1920×1080

### 阶段：PPT
- 文件：`output/presentation.pptx`
- 16:9 比例

## 7. 验收标准

1. 用户描述需求，AI 生成合理大纲（4-6页）
2. 用户确认大纲后，AI 正确选择布局
3. AI 正确匹配主题（技术类 → tech）
4. AI 生成的 HTML 符合设计系统约束
5. 脚本正确渲染截图（1920×1080）
6. 脚本正确组装 PPT（16:9）
7. 最终 PPT 可用 PowerPoint/WPS 打开

## 8. 当前修复范围

本次实现会重点修复以下运行级问题：

1. 生成链路必须从仓库根目录稳定定位，避免因工作目录不同而写错 `output/`
2. 每次生成前清理旧的 HTML 和 PNG 产物，避免历史文件污染最终 PPT
3. HTML 模板引擎需要支持嵌套循环，确保 `table` 等布局可以正常渲染

## 9. 设计系统回流规则

本项目后续所有版式问题，优先回流到设计系统主配置，而不是只在单页里临时修补。

1. 字号不统一，回流到 `presentationDefaults.typography`
2. 卡片文字偏上或水平居中，回流到 `presentationDefaults.alignment`
3. 截图出现白边，回流到 `renderingDefaults.screenshotTarget`
4. 输出污染，回流到 `renderingDefaults.clearOutputBeforeRun`
5. 字号、卡片内边距、垂直居中左对齐等布局回归，回流到 `design-system.json` 的 `regressionRules`，并由 `scripts/validate_design_system.py` 做自动检查
6. 收尾页若需要总结内容，优先拆分成 `timeline` 总结页 + 独立致谢页
7. 单页要点超过 5 条时，进入紧凑布局模式，优先避免把页脚挤出画布
