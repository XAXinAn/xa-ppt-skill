# ppt-skill AI 协作经验分享 演讲稿

## Slide 1 封面
大家好，今天我汇报的是我在实习里用 `ppt-skill` 做 AI 协作时的一些经验。

这次我不讲很空的想法，主要讲几件很具体的事：怎么对需求，怎么定边界，怎么把效率提上来，还有版本到底怎么管。

我踩过不少坑，今天讲的也都是这些坑里总结出来的东西。

---

## Slide 2 背景
先说一下我进公司前是怎么用 AI 的。

那时候我觉得 AI 就是一个日常办公自动化的工具。写课程设计、写论文，直接让它产出一个 Word。从 0 写一个 Web 课程设计，我说一步，它写一点代码，最后调整很多遍，才能得到一个相对满意的结果。

那时候我以为，AI 就是个高级打字机。我把需求说清楚，它把结果吐出来，说不清就改，改不对就再改。

进了公司之后才发现，这种"说一步做一步"的方式，根本算不上真正的 AI 协作。

---

## Slide 3 目录
先看目录。

第一部分是需求怎么对。这里最关键的一点，是先把文档写清楚，再让 AI 照着做，不然很容易各说各话。

第二部分是边界怎么定。从 MVP 开始，边跑边调，AI 的自由度还在，边界更清晰。

第三部分是效率怎么提。建立验证回路，让 AI 能看到结果，人能看到 AI 的推理过程。

最后是版本怎么管。建立可恢复性机制，协作过程有存档，出问题可以回滚。

---

## Slide 4 需求怎么对 问题
先看第一个问题，需求怎么对。

我一开始就碰到过一个偏差。我当时的原话是："当前阶段的目标应该是能输出一个自我介绍的PPT"。我实际想的是：先验证 ppt-skill 至少能做到这一步。但 AI 理解成了：ppt-skill 的需求目标就是专门输出自我介绍 PPT。结果它把需求说明和需求分析都大改了一遍，按"专门工具"的方向重做了整个方案。结果"当前阶段"这四个字在 AI 理解里完全消失了。

这件事不是说需求本身有问题，而是我说出来的版本和 AI 理解到的版本没对上，结果偏离了原意。

口头沟通最容易出的问题，就是你以为在对齐，其实AI 根本没理解你的意思。

---

## Slide 5 需求怎么对 原因
为什么会这样？

第一，AI 看到的上下文是有限的。你没说的背景，它不知道。上下文一压缩，这个问题会更明显。

第二，信息在传递的时候会损耗。想的、说的、AI 理解的，常常不是一回事。

第三，AI 说自己理解了，并不等于真的理解对了。最后还是要靠结果来判断。

所以这件事不能只靠口头对齐。口头说得再顺，最后也还是得落到文档上。

我后来基本就不太相信"差不多明白了"这种说法，还是看文档更踏实。

---

## Slide 6 需求怎么对 解决方式
我的做法很简单，既然口头对齐容易跑偏，就让 AI 先把理解写下来。

这里我会分三层看。

需求说明，先看它有没有抓住产品层面的目标。

需求分析，再看它对技术方案的判断有没有偏。

设计分析，这一层看得更具体一些，要确认它有没有把当前系统怎么组织、怎么落地、哪里有差异说清楚。

所以我的习惯就是先出文档，再写代码。这样至少能判断 AI 理解得对不对。

---

## Slide 7 需求对齐 案例
这一页是个比较典型的例子。

当时的场景是，我要做一个能自动生成 PPT 的 `skill`，中间做了多轮澄清，最后希望能输出 `.pptx`。

我当时的判断是，AI 已经理解得差不多了，就直接让它开始搭项目。

结果它真的搭了，但最后出来的东西是一个白底加几行字的 PPT。

问题出在哪？问题在于它没有搞清楚几个最基本的事。给谁用，什么算好，技术方案靠不靠谱，出错了怎么办。它甚至觉得写个 `python-pptx` 脚本就差不多够了。

后来我改了做法，按需求说明、需求分析、设计分析这三个标准，让它写出每个层面的理解，我再一条条去验证。让 AI 的理解变得可逐条检验。

最后它才确定了 HTML 渲染方案，`ppt-skill` 到现在还在用这套方案。

---

## Slide 8 边界怎么定 问题
第二部分讲边界。

边界这个东西，太死不行，太松也不行。

如果创意类任务框得太死，页面很容易千篇一律，看起来都差不多。反过来，如果严谨任务框得太松，AI 又很容易放飞自我，问题会更多。

所以真正要做的，不是简单地收紧或者放开，而是找到一个能让结果稳定，同时又留得出调整空间的边界。

---

## Slide 9 边界怎么定 原因
为什么边界总是难定？

因为它很难一次就定准。准备得再充分，也总会碰到没想到的情况。

还有一个现实点，AI 很少会主动提醒你规则有问题。它通常就是照着做，哪怕规则本身并不合理。

所以边界不是一次性设计完就结束了，它更像是一个不断校准的过程。

---

## Slide 10 边界怎么定 解决方式
这里我的做法是让人来做挑刺的人。

把必须保持一致的内容写成硬约束，先给 AI 划好活动范围。

从 MVP 开始，先用最基础的结构跑一版。边界不清楚没关系，看 AI 输出哪里出问题，就在哪里加约束。比如先只允许固定的几种布局，看 AI 输出样式太单一，就放开组件种类和参数；发现组件排版太乱，就加上容器分割的限制。多跑几轮，边跑边调，慢慢加功能上去。这样边界会越来越清晰，但不需要一开始就想清楚所有规则。

人始终负责判断和拍板。

所以这一部分最后可以收成一句话，人工来调边界，AI 在边界内做事。

---

## Slide 11 边界怎么定 案例
这一页也是一个很典型的例子。

同一个 `skill`，要生成述职报告、产品介绍、论文答辩这些不同类型的 PPT。

一开始我定义了很多组件和布局，以为种类够多，就能覆盖所有场景。

结果不是。内容换了，样式看起来还是差不多。想单独去掉图标，也做不到。

一开始我把组件和模板都写死，AI 只能往固定格子里填内容，跟直接写死一个填充文本的单 HTML 模板没区别。

后来我从 MVP 开始一点点放开，容器分割先限制正文区域和可放置组件，AI 也可以在容器里摆放组件，样式参数也开放得更多一些。

但真正跑起来以后又发现，AI 的生成开始变得过于随意，排版问题特别频繁，所以最后又收回到现在这个边界：页眉、页脚和正文大框架固定，正文区域通过容器和布局模板做分割，AI 主要是在约束范围内选择合适的布局、填充内容，并使用设计系统里开放的样式变量。

所以改完以后，页面表达确实比最初丰富，但最终还是要回到一个更稳的边界，不能完全交给 AI 自由排版。

---

## Slide 12 效率怎么提 问题
第三部分讲效率。

我之前很长一段时间，其实是把 AI 当成打字机在用。

但很多任务不是打字能解决的，尤其是需要用户反馈后继续改的任务。如果只靠文字描述，有时候十几轮对话都不一定能解决一个问题。

很常见的一种情况就是，你问它能不能看图，它回答说不好意思，它还没有识别图片的能力。这个时候你会发现，问题根本没进到真正的判断环节，而是在原地打转。

---

## Slide 13 效率怎么提 原因
为什么会卡住？

根因是：没有建立一个高效的验证回路。

AI 看不见自己生成的结果，人也看不见 AI 的推理过程。两个人隔着文字来回转述，细节丢失很多。

有些模型本身就没有多模态能力，天然做不了视觉任务。

所以这件事的关键，不是让文字描述更长，而是建立验证回路，让 AI 能看到结果，人能看到 AI 的推理过程。

---

## Slide 14 效率怎么提 解决方式
我的做法是建立"生成→可视化验证→反馈→修正"的验证回路。

视觉任务就用能识图的模型，让 AI 直接看。开发阶段靠视觉校准参数，验收阶段让 AI 自己看结果。

这样做之后，很多原本要靠人反复口述的细节，变成了可视化、可验证的调整过程。

所以这一部分最后我会记成一句话：AI 不只是会生成内容，它还能看图、读文件，这些能力别浪费。关键是把它们接进工作流里，形成闭环。

---

## Slide 15 效率怎么提 案例
这个案例比较直观。

当时我要定义组件的安全字数上限，还要检查渲染结果有没有组件溢出屏幕，或者文字被边框截断。

一开始的做法很笨，就是让 AI 凭感觉限制字数上限，我看到哪里有问题再口头反馈给它。

结果大量时间都耗在"字数太多了""字数太少了""这个组件被挤出去了"这些沟通上。

根因还是老问题，没有建立一个高效的验证回路——纯文字模型看不到渲染结果，人也看不见 AI 的调整效果。

后来我建立验证回路：AI 生成后直接看图反馈，而不是口头描述问题。比如调字体大小，AI 生成后直接看图调，比"再大一点"高效十倍。再比如校准安全字数上限，也是让 AI 直接抓取预设好的省略号溢出效果，逐轮调整，而不是口头描述"字数太多了"。

最后效果就比较稳定了。安全字数上限校准得很准，之前要反复很多轮对话的调整，现在 AI 一次就能处理。

---

## Slide 16 版本怎么管 问题
最后一部分是版本管理。

这个问题很现实。AI 不会自己管版本。你不提 commit，它就不会 commit，也不会提醒你该管版本了。

这件事看起来小，但真出问题的时候，影响会很大——协作过程没有存档点，出问题只能从头来。

我后来越来越觉得，版本管理不是收尾动作，而是整个协作过程里都得盯着的事。

---

## Slide 17 版本怎么管 原因
原因也不复杂。

AI 的工作流本身没有"保存点"概念。大多数工具默认不强制版本管理，所以它不说也不做。

上下文窗口有限，哪怕你前面说过，后面压缩掉了，AI 还是可能把这件事忘掉。

还有一个原因是人自己也可能没这个习惯。如果人自己就不重视版本管理，AI 更不可能替你坚持。

所以这件事最后还是得回到流程上，不能只靠临场提醒。核心是建立可恢复性机制，而不是"记得 commit"。

---

## Slide 18 版本怎么管 解决方式
我的做法是建立可恢复性机制：每个阶段结尾建立存档点。

存档点就是让 AI 每次完成一个阶段的改动后，主动 commit 代码并更新文档。这样下一阶段出了问题，可以直接回滚到上一个存档点，而不是从头开始重新沟通。

把工作规范提前写进系统提示词，让 AI 帮你记着该做的事。比如每次改动后主动 commit、先改文档再改代码、每个阶段结尾建立存档点。前提当然是，人自己先重视版本管理，AI 只是帮你执行。

所以这一部分我会记成一句话，用系统提示词帮 AI，也帮自己记住该做的事。核心是建立可恢复性机制，而不是"记得 commit"。

---

## Slide 19 版本怎么管 案例
这个案例是我印象最深的一个。

创意增强阶段刚完成一轮大改动，里面包括 `section` 机制、复合组件、安全字数上限。

当时我以为 `.codex` 里的 `skill` 只是普通文件夹，清理空间不会影响开发目录里的文件。

结果删完才发现，那个文件夹其实是开发目录的快捷方式，开发目录下的文件全被删了。

更麻烦的是，在这之前没有 commit，也没有更新文档，只能重新沟通修改需求。

根因就是，协作过程没有存档点，误删之后进度没法恢复——本质是缺少可恢复性机制。

后来我在每个阶段结尾建立存档点（commit + 文档），把"每次改动后主动 commit"写进系统提示词，也开始更认真地做手动 commit，并且坚持先改文档再改代码。

到现在为止，还没有再遇到同样的情况。

---

## Slide 20 谢谢观看
最后收一下。

今天这几页，其实讲的都是同一件事，就是不要指望 AI 自己把事情做对。

整体来看，AI 协作有一个闭环：

定义 → 生成 → 检查 → 修正 → 留痕

- 定义：需求文档化、可验证
- 生成：AI 按文档执行
- 检查：建立可视化验证回路
- 修正：从mvp开始快速迭代不返工
- 留痕：每个节点有存档，可回滚

做好这五步，AI 协作才能真正落地。

我这次汇报就到这里，谢谢大家。
